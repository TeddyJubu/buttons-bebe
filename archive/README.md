# Archive — retired / superseded artifacts

These trees and docs are kept for archaeology only. They are **not** part of
the live Buttons Bebe pipeline.

- Not deployed
- Not listed in CD `COMPONENTS` (`deploy/cd/source_release.py`)
- Not syntax-checked by `tools/verify_release.sh` (except accidental overlap
  via other live packages)

Do **not** revive, re-wire, or copy operational patterns from here without
revisiting the safety model in root `AGENTS.md` (and `RETIRED.md`).

Live source of truth: [`../AGENTS.md`](../AGENTS.md).

Moved here 2026-09 (pre-rebuild clutter retirement). Nothing was hard-deleted;
git history still has every prior path.
